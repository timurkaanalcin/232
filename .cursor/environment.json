{
  "name": "LiveTrack",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "next-dev",
      "command": "npm run dev",
      "description": "Next.js dev server on http://localhost:3000 with local Cloudflare bindings (D1 via initOpenNextCloudflareForDev). Durable Objects / realtime WebSockets require `npm run preview` (wrangler dev) instead."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
