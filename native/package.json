{
  "name": "canlisite-native",
  "version": "1.0.0",
  "private": true,
  "description": "CanlıSite Capacitor sarmalayıcı — üretim HTTPS sitesini WebView içinde açar.",
  "scripts": {
    "cap:sync": "cap sync android",
    "cap:add:android": "cap add android",
    "apk:debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4"
  }
}
